import NextHead from 'next/head';

const Head = () => (
    <NextHead>
        <title>UberSitter</title>

        <link rel="preconnect" href="https://fonts.gstatic.com" />
        {/* Poppins */}
    </NextHead>
)

export default Head